
import { AppBar } from '../AppBar/AppBar';
import { StyledHeader} from './Header.styled';

const Header = () => {

  return (
    <StyledHeader>
      <AppBar/>
    </StyledHeader>
  )
}

export default Header